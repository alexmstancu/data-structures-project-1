#include <cassert>
// and other #includes

using namespace std;

int main() {
    // dummy test file.

    // conditions can also be tested with assert(condition) such as:
    int x = 3;
    assert(x == 3);

    return 0;
    // return 0 from main if everything went according to plan
    // or something else otherwise.
}
